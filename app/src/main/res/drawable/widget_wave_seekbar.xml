<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background track: dim wave -->
    <item android:id="@android:id/background"
          android:drawable="@drawable/widget_wave_progress" />
    
    <!-- Progress fill: bright wave, clipped horizontally -->
    <item android:id="@android:id/progress">
        <clip android:drawable="@drawable/widget_wave_fill" />
    </item>
</layer-list>
